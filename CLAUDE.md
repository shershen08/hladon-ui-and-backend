@AGENTS.md

website will contain public pages and and pages for logged in; currently data will be in mocked JSON's but later will use separate DB, all data should be consumed via /api/..server side endpoints; 